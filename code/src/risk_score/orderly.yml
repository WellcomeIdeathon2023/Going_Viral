script: script.R

parameters:
    date:
      default: "NA"
    use_rt:
      default: TRUE
    use_projections:
      default: TRUE
    use_vaccine_coverage:
      default: TRUE
    use_sentiment:
      default: TRUE
      
artefacts: 
  - data:
      description: risk score
      filenames: risk_score.rds
      
packages:
  - ggplot2
  - dplyr
  - tidyr
  - epidemia
  
depends:
 collate_data:
   id: latest
   use:
     combined_dat.rds: combined_dat.rds
 epidemia_model:
   id: latest
   use:
     model_run.RData: model_run.RData

