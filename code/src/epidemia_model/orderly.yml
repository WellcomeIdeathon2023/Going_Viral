script: script.R

parameters:
    n_iter:
      default: 100
      
artefacts: 
  - data:
      description: model output
      filenames: model_run.RData
  - staticgraph:
      description: Figures from model fit and forecast
      filenames: 
      - plot_rt.png
      - plot_cases.png


packages:
  - epidemia
  - ggplot2
  - lubridate
  - tidyr
  - dplyr

depends:
 collate_data:
   id: latest
   use:
     combined_dat.rds: combined_dat.rds
